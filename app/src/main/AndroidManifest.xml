<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Package Visibility for Bank Apps (Android 11+ / API 30+) -->
    <queries>
        <package android:name="com.garanti.cepsubesi" />
        <package android:name="com.isbank.iscep" />
        <package android:name="com.akbank.android.apps.akbank_direkt" />
        <package android:name="com.ykb.android" />
        <package android:name="com.ziraat.ziraatmobil" />
        <package android:name="com.vakifbank.mobile" />
        <package android:name="com.enpara.mobile" />
        <package android:name="com.finansbank.mobile.cepsube" />
        <package android:name="com.papara" />
        <package android:name="com.kuveytturk.mobil" />
        <package android:name="com.teb" />
        <package android:name="com.denizbank.mobildeniz" />
        <package android:name="com.halkbank.sube" />
        <package android:name="tr.com.hsbc.hsbcturkey" />
        <package android:name="com.ingbanktr.ingmobil" />
    </queries>

    <application
        android:name=".AradaPayApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AradaPay"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AradaPay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Links for Shortcuts & In-App Routing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="aradapay"
                    android:host="*" />
            </intent-filter>

            <!-- Android App Launcher Shortcuts Meta-Data -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".service.AradaPayMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FileProvider for Sharing & Viewing PDF Receipts -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
